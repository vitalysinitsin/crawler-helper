{
  "name": "crawler-helper",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "crawler-helper": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  },
  "engines": {
    "node": ">=20"
  }
}
